server {
    listen 80;
    server_name weam.local;

    root /usr/share/nginx/html;
    index index.html;

    location /seo-content-gen/ {
        try_files $uri $uri/ /seo-content-gen/index.html;
    }

    # optional: root `/` ko handle mat karna
    location / {
        return 404;
    }
}
